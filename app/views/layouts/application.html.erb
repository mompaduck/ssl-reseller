<!DOCTYPE html>
<html lang="ko">
<head>
  <title>CertGate SSL 플랫폼</title>
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%# Tailwind CSS CDN (개발용) - 오류나서 임시 CDN %>
  <script src="https://cdn.tailwindcss.com"></script>

  <%# ★ ESBuild / Bun / Webpack 사용자 필수 JS %>
  <%= javascript_include_tag "application", defer: true, "data-turbo-track": "reload" %>

  <!-- ★★★ 바운스 스크롤 제거 + 배경색 보정 ★★★ -->
  <style>
    html, body {
      overscroll-behavior-y: none;      /* 위쪽 바운스 스크롤 방지 */
      background-color: #2563eb;        /* blue-600 (헤더 배경과 동일 색) */
      margin: 0;
      padding: 0;
    }
  </style>

  <%# Lucide Icons %>
  <script src="https://unpkg.com/lucide@latest"></script>

</head>

<body class="bg-white">
  <%= render "layouts/header" %>
  
  <%# 메인 컨텐츠 %>
  <main class="min-h-screen bg-white pt-16">
    <%# Flash 메시지 - 컨텐츠 최상단 %>
    <%= render "shared/flash_messages" %>
    
    <%= yield %>
  </main>
  
  <%= render "layouts/footer" %>
</body>
</html>
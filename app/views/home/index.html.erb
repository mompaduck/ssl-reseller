<%# app/views/home/index.html.erb %>

<div class="bg-white">
  <%# Hero Section %>
  <section class="relative overflow-hidden bg-gradient-to-b from-blue-50 to-white">
    <div class="max-w-7xl mx-auto px-6 py-16 sm:py-20 lg:px-8">
      <div class="mx-auto max-w-3xl text-center">
        <div class="inline-block mb-3">
          <span class="inline-flex items-center gap-x-1.5 rounded-full bg-blue-100 px-3 py-1.5 text-sm font-medium text-blue-700">
            <i data-lucide="circle" class="w-3 h-3 fill-blue-500"></i>
            10,000개 이상의 웹사이트가 신뢰
          </span>
        </div>
        
        <h1 class="text-5xl font-bold tracking-tight text-gray-900 sm:text-6xl">
          SSL로 웹사이트를 보호하세요
        </h1>
        
        <p class="mt-4 text-lg leading-7 text-gray-600 max-w-2xl mx-auto">
          비즈니스를 위한 전문 SSL 인증서. 빠른 발급, 합리적인 가격, 탁월한 지원.
        </p>
        
        <div class="mt-8 flex items-center justify-center gap-x-4">
          <a href="#pricing" class="rounded-lg bg-blue-600 px-6 py-3 text-base font-semibold text-white shadow-sm hover:bg-blue-700 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-blue-600 transition">
            시작하기
          </a>

        </div>
      </div>
    </div>
  </section>

  <%# Features Section %>
  <section class="py-16 sm:py-20" id="features">
    <div class="mx-auto max-w-7xl px-6 lg:px-8">
      <div class="mx-auto max-w-2xl text-center mb-12">
        <h2 class="text-base font-semibold leading-7 text-blue-600">필요한 모든 것</h2>
        <p class="mt-2 text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl">
          왜 저희를 선택해야 할까요?
        </p>
      </div>
      
      <div class="mx-auto max-w-2xl lg:max-w-none">
        <dl class="grid max-w-xl grid-cols-1 gap-x-8 gap-y-10 lg:max-w-none lg:grid-cols-3">
          <% [
            { icon: 'zap', title: '빠른 발급', description: 'DV 인증서는 몇 분 내 발급. OV/EV는 1-3 영업일 내 발급.' },
            { icon: 'shield', title: '업계 표준', description: 'SHA-2 알고리즘의 256비트 암호화. 모든 최신 브라우저와 호환.' },
            { icon: 'headphones', title: '최고의 지원', description: '연중무휴 고객 서비스 제공. 설치 및 관리를 위한 전문가 가이드.' }
          ].each do |feature| %>
            <div class="flex flex-col">
              <dt class="text-base font-semibold leading-7 text-gray-900">
                <div class="mb-4 flex h-10 w-10 items-center justify-center rounded-lg bg-blue-600">
                  <i data-lucide="<%= feature[:icon] %>" class="w-5 h-5 text-white"></i>
                </div>
                <%= feature[:title] %>
              </dt>
              <dd class="mt-1 flex flex-auto flex-col text-base leading-6 text-gray-600">
                <p class="flex-auto"><%= feature[:description] %></p>
              </dd>
            </div>
          <% end %>
        </dl>
      </div>
    </div>
  </section>

  <%# Pricing Section %>
  <section class="py-16 sm:py-20 bg-gradient-to-b from-blue-50 to-white" id="pricing">
    <div class="mx-auto max-w-7xl px-6 lg:px-8">
      <div class="mx-auto max-w-4xl text-center mb-12">
        <h2 class="text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl">요금제</h2>
        <p class="mt-3 text-lg leading-7 text-gray-600">
          비즈니스에 적합한 플랜을 선택하세요
        </p>
      </div>
      
      <div class="isolate mx-auto mt-10 grid max-w-md grid-cols-1 gap-6 lg:mx-0 lg:max-w-none lg:grid-cols-3">
        <%# DV Plan %>
        <div class="rounded-2xl p-6 ring-1 ring-blue-200 bg-white hover:ring-blue-300 transition">
          <h3 class="text-lg font-semibold leading-8 text-gray-900">도메인 검증 (DV)</h3>
          <p class="mt-3 text-sm leading-6 text-gray-600">개인 웹사이트 및 블로그용</p>
          <p class="mt-5 flex items-baseline gap-x-1">
            <span class="text-4xl font-bold tracking-tight text-gray-900">₩15,000</span>
            <span class="text-sm font-semibold leading-6 text-gray-600">/년</span>
          </p>
          <%= link_to "주문하기", main_app.products_path(validation_type: 'DV'), class: "mt-5 block w-full rounded-lg bg-blue-600 px-3 py-2.5 text-center text-sm font-semibold text-white shadow-sm hover:bg-blue-700 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-blue-600 transition" %>
          <ul role="list" class="mt-6 space-y-2.5 text-sm leading-6 text-gray-600">
            <% ['도메인 소유권 확인', '10분 내 발급', '256비트 암호화', '99.9% 브라우저 호환', '30일 환불 보장'].each do |item| %>
              <li class="flex gap-x-2.5 items-center">
                <i data-lucide="check" class="w-4 h-4 flex-none text-blue-600"></i>
                <%= item %>
              </li>
            <% end %>
          </ul>
        </div>

        <%# OV Plan - Featured %>
        <div class="rounded-2xl p-6 ring-2 ring-blue-600 bg-white shadow-lg transform scale-105">
          <div class="flex items-center justify-between">
            <h3 class="text-lg font-semibold leading-8 text-gray-900">기업/조직 검증 (OV)</h3>
            <span class="rounded-full bg-blue-600 px-2.5 py-0.5 text-xs font-semibold text-white">인기</span>
          </div>
          <p class="mt-3 text-sm leading-6 text-gray-600">비즈니스 및 조직용</p>
          <p class="mt-5 flex items-baseline gap-x-1">
            <span class="text-4xl font-bold tracking-tight text-gray-900">₩89,000</span>
            <span class="text-sm font-semibold leading-6 text-gray-600">/년</span>
          </p>
          <%= link_to "주문하기", main_app.products_path(validation_type: 'OV'), class: "mt-5 block w-full rounded-lg bg-blue-600 px-3 py-2.5 text-center text-sm font-semibold text-white shadow-sm hover:bg-blue-700 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-blue-600 transition" %>
          <ul role="list" class="mt-6 space-y-2.5 text-sm leading-6 text-gray-600">
            <% ['조직 신원 확인', '1-3 영업일 내 발급', '256비트 암호화', '조직 정보 표시', '$125만 보증', '우선 지원'].each do |item| %>
              <li class="flex gap-x-2.5 items-center">
                <i data-lucide="check" class="w-4 h-4 flex-none text-blue-600"></i>
                <%= item %>
              </li>
            <% end %>
          </ul>
        </div>

        <%# EV Plan %>
        <div class="rounded-2xl p-6 ring-1 ring-blue-200 bg-white hover:ring-blue-300 transition">
          <h3 class="text-lg font-semibold leading-8 text-gray-900">확장 검증 (EV)</h3>
          <p class="mt-3 text-sm leading-6 text-gray-600">이커머스 및 대기업용</p>
          <p class="mt-5 flex items-baseline gap-x-1">
            <span class="text-4xl font-bold tracking-tight text-gray-900">₩189,000</span>
            <span class="text-sm font-semibold leading-6 text-gray-600">/년</span>
          </p>
          <%= link_to "주문하기", main_app.products_path(validation_type: 'EV'), class: "mt-5 block w-full rounded-lg bg-blue-600 px-3 py-2.5 text-center text-sm font-semibold text-white shadow-sm hover:bg-blue-700 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-blue-600 transition" %>
          <ul role="list" class="mt-6 space-y-2.5 text-sm leading-6 text-gray-600">
            <% ['최고 수준의 인증', '녹색 주소 표시줄', '3-7 영업일 내 발급', '$150만 보증', '전담 계정 관리자'].each do |item| %>
              <li class="flex gap-x-2.5 items-center">
                <i data-lucide="check" class="w-4 h-4 flex-none text-blue-600"></i>
                <%= item %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <%# CTA Section %>
  <section class="relative isolate overflow-hidden bg-white py-16 sm:py-20">
    <div class="mx-auto max-w-7xl px-6 lg:px-8">
      <div class="mx-auto max-w-2xl text-center">
        <h2 class="text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl">
          시작할 준비가 되셨나요?
        </h2>
        <p class="mx-auto mt-4 max-w-xl text-lg leading-7 text-gray-600">
          오늘 웹사이트를 보호하고 고객과의 신뢰를 구축하세요.
        </p>
        <div class="mt-8 flex items-center justify-center gap-x-4">
          <%= link_to products_path, class: "inline-flex items-center gap-x-2 rounded-lg bg-blue-600 px-6 py-3 text-base font-semibold text-white shadow-sm hover:bg-blue-700 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-blue-600 transition" do %>
            인증서 둘러보기
            <i data-lucide="arrow-right" class="w-4 h-4"></i>
          <% end %>
        </div>
      </div>
    </div>
  </section>
</div>

<script>
  // Lucide 아이콘 초기화
  document.addEventListener('DOMContentLoaded', () => {
    lucide.createIcons();
  });
  
  // Turbo 페이지 로드 시에도 아이콘 초기화
  document.addEventListener('turbo:load', () => {
    lucide.createIcons();
  });
</script>

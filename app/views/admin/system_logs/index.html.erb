<div class="mb-8">
  <h1 class="text-2xl font-bold text-gray-900">System Logs</h1>
  <p class="mt-1 text-sm text-gray-500">시스템 레벨 로그를 확인합니다.</p>
</div>

<div class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
  <div class="overflow-x-auto">
    <table class="min-w-full divide-y divide-gray-200">
      <thead class="bg-gray-50">
        <tr>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">시간</th>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">레벨</th>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">소스</th>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">메시지</th>
        </tr>
      </thead>
      <tbody class="bg-white divide-y divide-gray-200">
        <% if @system_logs.any? %>
          <% @system_logs.each do |log| %>
            <tr class="hover:bg-gray-50">
              <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                <%= log.created_at.strftime("%Y-%m-%d %H:%M:%S") %>
              </td>
              <td class="px-6 py-4 whitespace-nowrap">
                <span class="px-2.5 py-0.5 inline-flex text-xs leading-5 font-semibold rounded-full <%= log.badge_color %>">
                  <%= log.formatted_level %>
                </span>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-sm font-mono text-gray-700"><%= log.source %></td>
              <td class="px-6 py-4 text-sm text-gray-900">
                <%= log.message %>
                <% if log.stack_trace.present? %>
                  <details class="mt-1">
                    <summary class="text-xs text-blue-600 cursor-pointer">Stack Trace</summary>
                    <pre class="mt-2 text-xs bg-gray-50 p-2 rounded border overflow-x-auto"><%= log.stack_trace %></pre>
                  </details>
                <% end %>
              </td>
            </tr>
          <% end %>
        <% else %>
          <tr>
            <td colspan="4" class="px-6 py-12 text-center text-gray-500">로그가 없습니다.</td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
  
  <% if @system_logs.total_pages > 1 %>
    <div class="px-6 py-4 border-t border-gray-200 bg-gray-50">
      <%= paginate @system_logs %>
    </div>
  <% end %>
</div>

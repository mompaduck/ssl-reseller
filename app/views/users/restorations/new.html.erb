<main class="min-h-screen bg-gradient-to-b from-blue-50 to-white flex items-center justify-center py-20 px-4">
  <div class="w-full max-w-md bg-white rounded-2xl shadow-xl border border-gray-100 overflow-hidden">
    <div class="bg-blue-600 px-8 py-6">
      <div class="flex items-center justify-center mb-2">
        <i data-lucide="refresh-cw" class="w-8 h-8 text-white"></i>
      </div>
      <h2 class="text-2xl font-bold text-white text-center">계정 복구</h2>
      <p class="text-blue-100 text-center mt-2 text-sm">삭제된 계정을 복구하시겠습니까?</p>
    </div>

    <div class="px-8 py-8">
      <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4 mb-6">
        <div class="flex items-start">
          <i data-lucide="alert-circle" class="w-5 h-5 text-yellow-600 mt-0.5 mr-3 flex-shrink-0"></i>
          <div>
            <h3 class="text-sm font-semibold text-yellow-800 mb-1">계정 복구 안내</h3>
            <p class="text-sm text-yellow-700">
              입력하신 이메일 <strong><%= @email %></strong>은(는) 삭제된 계정입니다.<br>
              계정을 복구하시면 이전의 모든 데이터와 권한이 복원됩니다.
            </p>
          </div>
        </div>
      </div>

      <%= form_with url: user_restoration_path, method: :post, class: "space-y-6" do |f| %>
        <%= f.hidden_field "user[email]", value: @email %>
        
        <div>
          <%= f.submit "계정 복구 메일 발송", 
              class: "w-full bg-blue-600 text-white py-3 rounded-lg font-semibold hover:bg-blue-700 transition shadow-md cursor-pointer" %>
        </div>
      <% end %>

      <div class="mt-6 text-center">
        <%= link_to "취소하고 돌아가기", new_user_registration_path, class: "text-sm text-gray-600 hover:text-gray-900" %>
      </div>
    </div>
  </div>
</main>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    lucide.createIcons();
  });
</script>
